#!/usr/bin/python3
def multiple_returns(sentence):
    leng = len(sentence)
    if lang == 0:
        return (leng, None)
    else:
        first = sentence[0]
        new = leng, first
        return (new)
